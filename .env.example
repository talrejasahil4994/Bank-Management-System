# Database Configuration (Option 1: Neon Database URL - Recommended)
# DATABASE_URL=postgresql://username:password@ep-xyz.us-east-2.aws.neon.tech/BANK?sslmode=require

# Database Configuration (Option 2: Individual Parameters - Fallback)
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5433
DB_NAME=Bank

# Application Configuration
PORT=5000
NODE_ENV=production

# For Production (Cloud Database)
# DATABASE_URL=postgresql://user:password@host:port/database

# CORS Configuration (for production)
# FRONTEND_URL=http://localhost:3000
